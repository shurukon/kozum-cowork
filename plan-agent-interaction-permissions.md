# خطة إصلاح تفاعل المستخدم والصلاحيات ودورة Agent Loop

## الهدف

إصلاح مسار الأدوات التي تحتاج إدخالًا أو موافقة من المستخدم، بحيث تظهر أسئلة `ask_user_question` وطلبات الصلاحية داخل المحادثة وتوقف دورة الوكيل مؤقتًا حتى وصول الرد، مع تطبيق سياسة صلاحيات دقيقة ومختلفة بين Cowork وCode. كما ستُصلح حالة التشغيل وزر الإيقاف ليبقيا فعالين طوال دورة الأدوات والـthinking حتى النهاية الفعلية للـturn، ثم تُجرى اختبارات حقيقية واسعة للأدوات والـbrowser والـcomputer-use مع حفظ الأدلة والنتائج.

## المرحلة الأولى: تدقيق المسار الحالي وتثبيت العقود

1. مراجعة `shared/types.ts` و`AskBroker` و`SessionManager` و`sessionReducer` و`ChatView` و`Message` و`QuestionFormView` و`PermissionBanner` لتثبيت دورة الحدث من backend إلى الواجهة ثم عودة الرد عبر IPC.
2. تحديد سبب فشل كل فئة من الأدوات التي تتطلب إدخالًا: هل المشكلة في عدم إصدار الحدث، أو عدم تسجيل الطلب، أو عدم ربط `requestId`، أو مسح الحالة قبل انتهاء الانتظار.
3. الحفاظ على endpoints الحالية (`sessions:reply` و`sessions:cancel`) وعدم إنشاء مسار بديل يكرر منطق الرد.
4. وضع اختبار regression قبل التعديل لمسار سؤال وموافقة وإلغاء، مع عدم تعطيل الاختبارات الحقيقية الحالية أو استبدالها بـmock جديد.

## المرحلة الثانية: واجهة سؤال المستخدم داخل المحادثة

1. ضمان أن أداة `ask_user_question` تصدر حدث `question` موحدًا يحوي السؤال والخيارات و`multiSelect` و`requestId` ومرجع الرسالة/الدورة.
2. تطوير `QuestionFormView` ليعرض السؤال inline داخل activity timeline في Cowork وCode، ويدعم الاختيارات الأحادية والمتعددة، وزر إرسال واضح، وإجابة نصية حرة عندما لا تكون الخيارات كافية أو عندما يحدد عقد الأداة ذلك.
3. منع الإرسال المكرر وتعطيل النموذج بعد الإجابة، وإظهار حالة `answered` أو `cancelled` بدل اختفاء الطلب بصمت.
4. ربط الإجابة مباشرة بـ`bridge.sessions.reply`، مع معالجة أخطاء IPC داخل chat، ومعالجة إلغاء السؤال عند إيقاف turn أو إغلاق الجلسة.
5. جعل حالة الجلسة `waiting_input` واضحة في المخفض والواجهة، بحيث لا يُعامل انتظار المستخدم كـidle ولا يُفقد الطلب أثناء إعادة الرسم أو إعادة إرفاق session.

## المرحلة الثالثة: سياسة الصلاحيات الجديدة

1. توسيع عقد `permission_request` ليعرض ثلاثة أفعال واضحة: `allow once` و`allow always` و`deny`، مع قيمة آمنة عند وصول رد قديم مثل `yes/no`.
2. في Cowork، تطبيق الآتي داخل الجلسة الحالية فقط:
   - `allow once`: السماح باستدعاء الأداة الحالي فقط.
   - `allow always`: تسجيل موافقة الأداة أو فئة الأداة في حالة session الذاكرية حتى نهاية الجلسة.
   - `deny`: رفض الاستدعاء وإرجاع نتيجة منظمة إلى الوكيل.
3. إعادة تهيئة موافقات `allow always` عند إنشاء session جديدة، وعدم تخزينها في إعدادات عامة أو مشاركتها بين Cowork وCode أو بين جلسات مختلفة.
4. في Code، تطبيق السياسة حسب `PermissionMode`:
   - `plan`: قراءة وتحليل الملفات وصنع الخطة فقط، مع رفض الأدوات ذات التغيير أو الآثار الجانبية.
   - `ask_permission`: طلب موافقة لكل استدعاء محمي.
   - `accept_edits`: السماح بتعديلات الملفات العادية تلقائيًا، مع طلب موافقة للأوامر الخطرة والحذف والعمليات وbrowser/computer والآثار الخارجية.
   - `bypass_permissions`: التنفيذ دون طلب موافقة.
5. إبقاء `PermissionBanner` و`ToolCard` inline، وتحديثهما لعرض الأزرار الثلاثة دون Dialog أو نافذة منفصلة.
6. ضمان أن `allow always` لا يتجاوز حدود الجلسة ولا يسمح تلقائيًا بأداة أو فئة غير مقصودة إذا تغيرت مدخلاتها إلى عملية خطرة.

## المرحلة الرابعة: إصلاح دورة التشغيل وزر الإيقاف

1. فصل مفهوم `model streaming` عن `agent turn running`: عدم استخدام `streamingMessageId` وحده لتحديد `busy`.
2. اعتماد `session_status: running` حتى نهاية `runAgentLoop` الفعلية، بما في ذلك tool calls وpermission waits وquestion waits.
3. تعديل reducer بحيث لا يمسح `streaming` أو `pendingQuestions` أو `pendingPermissions` عند `turn_end` الوسيط إذا كانت دورة الأدوات ما زالت مستمرة؛ المسح النهائي يتم عند `idle` أو `cancelled` أو `error` النهائي فقط.
4. إبقاء زر Stop ظاهرًا وفعالًا في Cowork وCode خلال التفكير، تنفيذ الأدوات، انتظار السؤال، وانتظار الموافقة، مع منع إرسال turn جديد بالتوازي.
5. جعل `sessions.cancel` يرسل abort إلى الأداة المنتظرة أو AskBroker، وينهي الانتظار دون deadlock، ويغلق أي طلب pending بحالة cancelled واضحة.
6. إضافة اختبار خاص لتسلسل: thinking → tool_start → tool_end/permission → tool_start ثانٍ → turn_end → session_status idle، والتأكد من بقاء زر الإيقاف حتى الحدث النهائي.

## المرحلة الخامسة: اختبار حقيقي شامل للأدوات

سيُستخدم مفتاح Kilo الذي قدمه المستخدم فقط عبر متغير بيئي/إعداد محلي غير متعقب، ولن يُكتب في source أو logs أو commit. سيتم التحقق أولًا من provider/model والاتصال الفعلي، ثم تشغيل agent tasks حقيقية بدل الاكتفاء باختبارات شكلية.

### مصفوفة الاختبار

| المجال | الاختبار الحقيقي | الدليل المطلوب |
|---|---|---|
| Filesystem | إنشاء وقراءة وتعديل وحذف ملفات داخل workspace مؤقت، مع اختبار plan وaccept edits وdeny | سجل الأحداث والملفات قبل/بعد |
| Shell/process | أوامر آمنة مثل إنشاء مخرجات وقراءةها، timeout، cancellation، ورفض أمر خطير | tool cards ونتيجة الأمر وحالة الإلغاء |
| Browser | فتح صفحة اختبار محلية، screenshot، استخراج محتوى، navigation، preview، وإغلاق lifecycle | screenshots وURLs وسجل browser state |
| Computer-use | تحريك/نقر/كتابة على نافذة اختبار محلية، screenshot، ثم cancellation أثناء التنفيذ | screenshots قبل/بعد وسجل X11/backend |
| MCP/connectors | listing ونداء server متاح فعليًا أو fixture محلي حقيقي، مع permission gates | request/response وtool events |
| Skills/plugins | تحميل عنصر اختباري فعلي من مسار المستخدم، تفعيله، استدعاؤه، ثم تعطيله | catalog state وtool result |
| Ask user | مهمة تطلب قرارًا من المستخدم ثم متابعة الوكيل بعد `reply` | question event، إجابة المستخدم، واستمرار turn |
| Permissions | allow once/always/deny في Cowork، وكل أوضاع Code الأربعة | أحداث permission والنتيجة النهائية |
| Preview/artifacts | إنشاء HTML وصورة وPDF وملف نصي ثم فتحها في Preview | لقطات للـPreview والمسارات |
| Long-running loop | مهمة متعددة الخطوات تستخدم عدة أدوات وتُلغى أثناء tool execution | timeline، session status، وغياب duplicate calls |

سيتم بناء تقرير نتائج آلي بصيغة JSON/Markdown يميز بوضوح بين `passed` و`failed` و`blocked-by-environment`، مع عدم اعتبار الأدوات التي تتطلب خدمة خارجية متاحة ناجحةً اعتمادًا على mock. الأدوات الخطرة ستُختبر داخل مجلد مؤقت وببيانات غير حساسة وبموافقات صريحة، أما أي فعل خارجي غير قابل للعكس فلن يُنفذ دون تأكيد مستقل.

## المرحلة السادسة: الاختبارات والقبول

1. تشغيل `npm run typecheck` بعد كل مجموعة تغييرات.
2. تشغيل اختبارات الوحدات والتكامل والمكونات دون حذف التغطية القائمة.
3. إضافة اختبارات مكونية لـQuestionForm وPermissionBanner/ToolCard وزر Stop، واختبارات تكاملية لـAskBroker وsession manager وpermission policy وsession-scoped approvals.
4. تشغيل `npm run verify` و`npm run build`.
5. تشغيل الاختبار الحقيقي الشامل بالـprovider الذي قدمه المستخدم، وحفظ screenshots وraw event logs وملف التقرير خارج source أو في مجلد أدلة مستبعد من Git.
6. مراجعة `git diff --check`، التأكد من عدم وجود مفتاح API أو بيانات حساسة في diff، ثم commit وpush إلى `origin/main` فقط بعد وصول جميع الاختبارات البرمجية إلى صفر فشل وتوثيق أي قيد بيئي حقيقي.

## افتراضات ومخاطر

- يوجد backend فعلي لـ`AskBroker` وIPC للرد، وسيكون التعديل الأساسي توسيع العقد والحالة وليس اختراع endpoint جديد.
- الموافقات `allow always` مطلوبة على مستوى جلسة Cowork فقط كما حدد المستخدم، ولا ينبغي أن تنتقل إلى Code أو session لاحقة.
- نجاح اختبار computer-use يعتمد على توفر X11/xdotool/ffmpeg في بيئة الاختبار؛ عند غيابها سيُسجل ذلك كقيد بيئي مع اختبار المسار البرمجي والإلغاء، لا كنجاح وهمي.
- بعض أدوات MCP أو connectors قد تحتاج خادمًا أو حسابًا خارجيًا غير متاح؛ لن يتم تعويض ذلك بـmock، وسيُذكر اسم الأداة وسبب الحجب في التقرير.
- لن يتم إرسال أو تنفيذ أي إجراء خارجي حساس تلقائيًا. أي اختبار يحتاج نشرًا أو حذفًا خارج workspace المؤقت يتطلب تأكيدًا صريحًا من المستخدم.
